import React from 'react'
import { ContainerForm } from './style'

const ContainerSml = ({children}) => {
  return (
    <ContainerForm>
        {children}
    </ContainerForm>
  )
}

export default ContainerSml